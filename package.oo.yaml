version: 0.0.1
scripts:
  # Script executed when the container is first loaded
  # eg: bootstrap: poetry add pandas
  bootstrap: |
    npm install
    poetry install --no-root
dependencies:
  {}
name: github-star-email
description: Extract the email addresses of all users who have starred the
  repository from a specific warehouse
displayName: Github Star Email
repository: https://github.com/BlackHole1/github-star-email
icon: ":simple-icons:github:#7494A3:"
